# AegisOps 正式 Release 验收清单

## 1. 文档目的

本文档用于作为 AegisOps 每次“正式发布版本”前的统一验收口径。

它回答的不是“代码是不是已经写了很多”，而是以下更关键的问题：

1. 当前版本是否真的达到可发布状态
2. 哪些问题属于正式 Release 的阻塞项
3. 哪些能力必须形成最小闭环
4. 版本号、Tag、发布说明应该如何收口

本文档适用于：

- 正式生产版本发布
- 公开展示版本发布
- 面试项目的稳定演示版本发布
- 个人长期自用版本的阶段性封版

---

## 2. 发布分级建议

为了避免“只要能跑就打正式版”，建议先把发布分成三类：

### 2.1 开发版

适用于：

- 功能开发中
- 工作区未清理
- 允许存在未完成模块
- 允许前端或后端局部不稳定

特征：

- 可以提交代码
- 可以内部预览
- 不应打正式 Release Tag

### 2.2 候选版 RC

适用于：

- 主要功能已经闭环
- 构建和测试基本通过
- 只剩少量体验或文档问题

特征：

- 可打 `rc` 类型 Tag
- 可做完整演示和回归
- 仍不建议直接定义为正式生产版

### 2.3 正式 Release

适用于：

- 版本基线清晰
- 功能闭环明确
- 构建、测试、启动、冒烟全部通过
- 没有已知高风险阻塞项

特征：

- 可以打正式语义化版本 Tag
- 可以写发布说明
- 可以作为稳定演示版本、交付版本或长期使用版本

---

## 3. 使用方式

建议把清单分成三层：

- `P0 必须通过`
- `P1 建议通过`
- `P2 可延期`

判定原则：

- 只要有任意一个 `P0` 未通过，本次不得定义为正式 Release
- `P1` 未通过时，原则上不建议发正式版，除非明确降级为 RC
- `P2` 可根据节奏延后，但应在发布说明中注明已知限制

---

## 4. P0 必须通过项

## 4.1 代码与版本基线

- [ ] 发布目标 commit 已明确，且只能对应一个确定的 Git 提交
- [ ] 发布前工作区为干净状态，没有未提交修改和未跟踪文件
- [ ] 发布 commit 已合并到主分支或正式发布分支
- [ ] 本次版本号已确定，并与发布说明一致
- [ ] 前端版本号、文档版本号、Tag 版本号没有互相打架
- [ ] 若本次要发布到远端，确认本地提交已准备推送
- [ ] 本次发布范围已明确，不把“开发中功能”误算进正式能力

建议的基线命令：

```powershell
git status --short
git log --oneline -10
git tag --list
```

判定标准：

- `git status` 必须为空或只有明确允许忽略的构建产物
- 若还有大量业务代码未提交，不得打正式版

## 4.2 后端构建与测试

- [ ] 后端依赖可正常安装和解析
- [ ] 后端可从干净环境启动
- [ ] 后端全量单元测试通过
- [ ] 没有启动即 panic、初始化失败、迁移失败的问题
- [ ] 生产模式与测试模式的关键差异已知且可控

建议命令：

```powershell
$env:GOCACHE = "$PWD\.gocache"
go test ./...
go run ./cmd/aegisops
```

若使用配置文件启动，也应至少验证：

- [ ] 配置文件路径正确
- [ ] SQLite 数据目录可创建
- [ ] `/healthz` 正常
- [ ] `/readyz` 正常

## 4.3 前端构建与启动

- [ ] 前端依赖安装正常
- [ ] TypeScript 构建通过
- [ ] Vite 生产构建通过
- [ ] 本地开发启动正常
- [ ] 没有构建时依赖本机绝对路径的错误
- [ ] 打包产物可用于部署或预览

建议命令：

```powershell
Set-Location frontend
npm install
npm run build
npm run dev
```

判定标准：

- `npm run build` 失败属于正式 Release 阻塞项
- 构建失败时，不得打正式版本 Tag

## 4.4 核心功能闭环

发布前必须先明确“本次正式版本承诺哪些功能”。

凡是写进 README、发布说明或产品说明里的能力，都应至少通过一轮最小闭环验收。

### 认证与权限闭环

- [ ] 默认管理员初始化逻辑正常
- [ ] 登录成功
- [ ] `auth/me` 正常返回当前用户
- [ ] 普通用户访问无权限接口会返回 403
- [ ] 高风险接口权限校验有效

### Secret 与凭据闭环

- [ ] 可以创建 Secret
- [ ] 可以在 Host / Docker / Registry / Nginx 等资源中绑定 Secret
- [ ] 使用 Secret 的动作可成功执行
- [ ] 前端不应直接展示真实敏感值

### 主机闭环

- [ ] 可以创建主机
- [ ] 可以发起 SSH 测试
- [ ] SSH 测试结果可在任务中心看到
- [ ] 主机状态或可用性记录可查询

### Docker 闭环

- [ ] 可以创建 Docker 节点
- [ ] 可以测试连接
- [ ] 可以查看容器列表或模拟容器数据
- [ ] 容器启动 / 停止 / 重启动作有结果反馈

### Registry 闭环

- [ ] 可以创建镜像仓库配置
- [ ] 可以测试连接
- [ ] 可以查询仓库 / 标签 / Manifest

### Service 发布闭环

- [ ] 可以创建服务定义
- [ ] 可以发起发布
- [ ] 发布生成任务
- [ ] 任务有步骤和日志
- [ ] 生成版本记录、发布记录、实例记录
- [ ] 健康检查结果可追踪
- [ ] 失败时有清晰状态和原因

### Nginx 闭环

- [ ] 可以创建 Nginx 节点
- [ ] 可以测试节点
- [ ] 可以发布配置
- [ ] 可以 reload
- [ ] 可以 rollback
- [ ] 相关动作进入任务或可追踪链路

### 告警与通知闭环

- [ ] 可以创建通知通道
- [ ] 可以发送测试通知
- [ ] 可以创建告警规则
- [ ] 异常事件会产生告警记录
- [ ] 通知发送记录可查询

### 任务中心闭环

- [ ] 关键长耗时动作会生成任务
- [ ] 任务详情能看到步骤
- [ ] 任务详情能看到日志
- [ ] 成功 / 失败状态可追踪

### 导出与备份闭环

如果本次版本对外宣称已支持导出 / 备份，则必须通过：

- [ ] 资源导出成功
- [ ] 故障排查包导出成功
- [ ] 备份创建成功
- [ ] 下载接口可用
- [ ] 脱敏行为符合预期

## 4.5 安全与配置基线

- [ ] 生产环境不再使用默认管理员密码
- [ ] 生产环境 `JWT secret` 已替换
- [ ] 生产环境 `Secret key` 已替换
- [ ] 没有把敏感信息硬编码到仓库
- [ ] 无权限用户不能读取敏感配置
- [ ] 默认导出 / 备份不会泄露明文 Secret
- [ ] 高风险动作有审计记录

对 AegisOps 特别重要的发布前确认：

- [ ] README 中的默认账号密码仅用于本地开发说明，不可直接作为生产实际配置
- [ ] 通知渠道、Webhook、邮箱密码等敏感信息不应明文暴露给普通查看者
- [ ] 非脱敏导出若已开放，必须有单独权限和二次确认

## 4.6 部署与回滚准备

- [ ] 发布说明里写清本次版本号
- [ ] 写清本次变更范围
- [ ] 写清已知限制
- [ ] 写清升级方式
- [ ] 写清回滚方式
- [ ] 上一个稳定 Tag 可定位
- [ ] 至少保留一份发布前数据库备份

最低回滚要求：

- [ ] 知道回滚到哪个 Git Tag
- [ ] 知道是否需要回滚数据库
- [ ] 知道是否需要恢复配置或构建产物

## 4.7 发布后冒烟检查

- [ ] 首页或登录页可打开
- [ ] 登录成功
- [ ] 核心导航可进入
- [ ] 至少一条关键任务链路可跑通
- [ ] `/healthz` 正常
- [ ] `/readyz` 正常
- [ ] 至少一个高风险动作的权限拒绝行为正常
- [ ] 关键日志和错误提示没有明显异常

---

## 5. P1 建议通过项

- [ ] 后端核心模块具备基本单元测试覆盖
- [ ] 前端关键页面做过至少一轮人工回归
- [ ] 关键 API 有真实而不是纯 mock 的联调验证
- [ ] SQLite 写压力下没有明显 `database is locked` 高频问题
- [ ] 至少做过一次“失败场景”演练，而不是只测成功路径
- [ ] Windows 本地环境验证通过
- [ ] 如需 Linux 部署，Linux 环境也验证过
- [ ] 构建产物目录结构已确认可部署
- [ ] 版本发布说明文档已整理

这些项若未通过，不一定绝对阻塞，但非常容易导致“版本能发，发完不好用”。

---

## 6. P2 可延期项

- [ ] E2E 自动化冒烟
- [ ] 浏览器兼容性回归
- [ ] 压测与容量评估
- [ ] 灰度发布方案
- [ ] 多实例部署验证
- [ ] 外部监控接入
- [ ] 完整变更日志页面

这些能力更偏平台成熟度，不一定要求第一次正式版全部具备。

---

## 7. AegisOps 特定阻塞项清单

以下问题一旦存在，当前版本不应定义为正式 Release：

- [ ] 前端 `npm run build` 失败
- [ ] 后端 `go test ./...` 失败
- [ ] 工作区不干净且仍有核心业务改动未提交
- [ ] README、文档声明的能力与真实实现不一致
- [ ] 关键页面只能靠 mock 正常，真实后端无法闭环
- [ ] 高风险动作没有权限保护
- [ ] Secret 或通知敏感配置可被明文查看
- [ ] 发布、Nginx、生效、导出等关键动作失败时不可追踪
- [ ] 版本号已经计划发布，但 Tag 尚未与目标 commit 对齐

---

## 8. 正式 Release 执行流程模板

建议每次正式发版按以下顺序操作：

1. 冻结本次发布范围
2. 清理工作区并完成所有必要提交
3. 跑后端全量测试
4. 跑前端生产构建
5. 跑最小功能闭环冒烟
6. 更新版本号
7. 更新 README / 发布说明 / 变更摘要
8. 创建 release commit
9. 打正式 Tag
10. 推送主分支和 Tag
11. 发布后再次做冒烟检查

建议命令模板：

```powershell
git status --short
$env:GOCACHE = "$PWD\.gocache"
go test ./...
Set-Location frontend
npm run build
Set-Location ..
git tag -a vX.Y.Z -m "release: vX.Y.Z"
git push origin main
git push origin vX.Y.Z
```

---

## 9. 发布记录模板

建议每次发版都补一份最小记录：

### 发布信息

- 版本号：
- Tag：
- 发布日期：
- 发布人：
- 对应 commit：

### 验收结果

- 后端测试：
- 前端构建：
- 核心功能冒烟：
- 权限冒烟：
- 回滚方案确认：

### 发布范围

- 本次新增：
- 本次修复：
- 本次不包含：

### 已知问题

- 问题 1：
- 问题 2：

---

## 10. 当前项目达到正式 Release 的最低门槛建议

结合 AegisOps 当前阶段，建议正式 Release 的最低门槛至少是：

- [ ] `go test ./...` 全绿
- [ ] `frontend npm run build` 全绿
- [ ] 工作区清理干净
- [ ] 本次版本范围明确
- [ ] 登录、主机、Docker、Service、Nginx、任务中心至少完成一轮真实冒烟
- [ ] README 中列出的主要能力没有明显“写了但不能用”的情况
- [ ] 当前版本号与 Tag 一致
- [ ] 至少保留上一个稳定版本可回滚

如果上述任一项未通过，建议将当前状态定义为：

- 开发版
- 候选版 RC
- 阶段演示版

而不是正式 Release。

---

## 11. 一句话结论

正式 Release 的标准，不是“功能已经很多”，而是：

`版本基线清楚 + 构建测试通过 + 核心闭环成立 + 安全边界清晰 + 可回滚`

只要这五件事里还有明显短板，AegisOps 就更适合继续收口，而不是仓促打正式发布标签。
